services:
  advisor_service:
    build:
      context: .
      dockerfile: Dockerfile
    image: my-trip-advisor:latest
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - OPENAI_API_KEY
